# GitHub Sync setup

GitHub Pages:
Settings → Pages → Source → GitHub Actions.

Общий игровой JSON:
`data/game.json`

Чтобы изменить общий рынок/объявление:
1. Измени `data/game.json`.
2. Сделай commit в `main`.
3. GitHub Actions автоматически проверит JSON и заново опубликует сайт.
